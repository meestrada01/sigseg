import other from './other';
import pages from './pages';

// ==============================|| MENU ITEMS ||============================== //
/* import useAuth from 'hooks/useAuth';
const {  componente } = useAuth(); */
const menuItems = {
    items: [pages, other]
};

export default menuItems;
